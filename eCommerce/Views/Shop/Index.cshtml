@model eCommerce.DATA.Models.ProductModel

<h2>Ürün Listesi</h2>


<div class="container-fluid fruite py-5">

    <div class="container py-5">
        <h1 class="mb-4">Fresh fruits shop</h1>

        <div class="row g-4">
            <div class="col-lg-12">
                <form method="get" asp-controller="Shop" asp-action="Index">
                    <div class="row g-4">
                        <div class="col-xl-3">
                            <div class="input-group w-100 mx-auto d-flex">
                                <input type="search" class="form-control p-3" name="keyword" placeholder="keywords" value="@Model.SelectedKeyword" />
                                <button type="submit" class="input-group-text p-3"><i class="fa fa-search"></i></button>
                            </div>
                        </div>
                        <div class="col-6"></div>
                    </div>
                </form>

                </div>
                <div class="row g-4">
                    <div class="col-lg-3">
                        <div class="row g-4">
                            <form method="get" asp-action="Index" class="mb-4">
                                <div class="col-lg-12">
                                    <div class="mb-3">
                                        <h4>Kategoriler</h4>
                                        <ul class="list-unstyled fruite-categorie">
                                            @foreach (var item in Model.Categories)
                                            {
                                                <li>
                                                    <div class="d-flex justify-content-between fruite-name">
                                                        <a href="@Url.Action("Index", "Shop", new { categoryID = item.CategoryId })">
                                                            <img src="@item.ImageUrl" width="20" height="20" class="me-2" />
                                                            @item.CategoryName
                                                        </a>
                                                    <span>@(item.Products == null ? "0" : item.Products.Count())</span>
                                                    </div>
                                                </li>
                                            }
                                        </ul>
                                    </div>
                                </div>

                                <div class="col-lg-12">
                                    <div class="mb-3">
                                        <h4 class="mb-2">Price</h4>
                                        <input type="range" class="form-range w-100" id="rangeInput" name="price" min="0" max="500" value="@Model.SelectedPrice" oninput="amount.value = rangeInput.value">
                                        <output id="amount">@Model.SelectedPrice ₺</output>
                                    </div>
                                </div>

                                <div class="col-lg-12">
                                    <button type="submit" class="btn btn-primary w-100">Filtrele</button>
                                </div>
                            </form>
                           
                            <div class="col-lg-12">
                                <div class="position-relative">
                                    <img src="img/banner-fruits.jpg" class="img-fluid w-100 rounded" alt="">
                                    <div class="position-absolute" style="top: 50%; right: 10px; transform: translateY(-50%);">
                                        <h3 class="text-secondary fw-bold">Fresh <br> Fruits <br> Banner</h3>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="col-lg-9">
                        <div class="row g-4 justify-content-center">
                            <div class="row">
                                @foreach (var product in Model.Products)
                                {
                                    <div class="col-md-4 mb-4">
                                        <h4>@product.ProductName</h4>
                                        <p>Kategori: @product.Category?.CategoryName</p>
                                    <img src="~/@product.ImageUrl" alt="@product.ProductName" class="img-fluid" />
                                        <p>Fiyat: @product.Price ₺</p>
                                        <a href="@Url.Action("Detail", "Shop", new { id = product.ProductId })">Detay</a>
                                    </div>
                                }
                            </div>


@* 
                            <div class="    " style="top: 10px; left: 10px;">Fruits</div>
 *@
                            


                          @*   <div class="col-12">
                                <div class="pagination d-flex justify-content-center mt-5">
                                    <a href="#" class="rounded">«</a>
                                    <a href="#" class="active rounded">1</a>
                                    <a href="#" class="rounded">2</a>
                                    <a href="#" class="rounded">3</a>
                                    <a href="#" class="rounded">4</a>
                                    <a href="#" class="rounded">5</a>
                                    <a href="#" class="rounded">6</a>
                                    <a href="#" class="rounded">»</a>
                                </div>
                            </div> *@
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>